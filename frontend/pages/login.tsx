import React from 'react';
import LoginPageComponent from '../components/LoginPage';

const LoginPage: React.FC = () => { // Renamed to LoginPageComponent to avoid naming conflict
  return (
    <LoginPageComponent />
  );
};

export default LoginPage;